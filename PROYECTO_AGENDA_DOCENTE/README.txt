Proyecto: PROYECTO_AGENDA_DOCENTE

Instrucciones rápidas:
1. Crear un entorno virtual (opcional):
   python -m venv venv
   source venv/bin/activate   (Linux/Mac) or venv\Scripts\activate (Windows)

2. Instalar Flask:
   pip install flask

3. Ejecutar:
   python app.py

El proyecto usa SQLite y la base ya viene preconfigurada en database/agenda_docente.db
